#!/usr/bin/env node
/**
 * These are imported by the JSDoc in the transformers
 */
type Meta = {
    name: string;
    source: string;
    description?: string;
    license: string;
};
type Transformer = (input: string, meta: Meta) => Promise<string> | string;

export { Meta, Transformer };

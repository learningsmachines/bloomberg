import { type RouteManifest } from './index';
export declare type RoutingConvention = 'flat-files' | 'flat-folders' | 'hybrid';
export declare type MigrateOptions = {
    convention: RoutingConvention;
    force: boolean;
};
export declare function migrate(sourceDir: string, targetDir: string, options?: MigrateOptions): void;
export declare function convertToRoute(routes: RouteManifest, sourceDir: string, id: string, parentId: string, routePath: string, index: boolean, convention: RoutingConvention): string;

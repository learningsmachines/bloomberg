export declare const clientHint: {
    readonly cookieName: "CH-reduced-motion";
    readonly getValueCode: "window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'reduce' : 'no-preference'";
    readonly fallback: "no-preference";
    readonly transform: (value: string | null) => "reduce" | "no-preference";
};
/**
 * Subscribe to changes in the user's motion preference. Optionally pass
 * in a cookie name to use for the cookie that will be set if different from the
 * default.
 */
export declare function subscribeToMotionChange(subscriber: (value: 'reduce' | 'no-preference') => void, cookieName?: string): () => void;

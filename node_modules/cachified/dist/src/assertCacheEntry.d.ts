import type { CacheMetadata } from './common';
export declare function logKey(key?: string): string;
export declare function assertCacheEntry(entry: unknown, key?: string): asserts entry is {
    metadata: CacheMetadata;
    value: unknown;
};

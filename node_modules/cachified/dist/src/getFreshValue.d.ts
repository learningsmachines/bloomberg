import { Context, CacheMetadata } from './common';
import { Reporter } from './reporter';
export declare function getFreshValue<Value>(context: Context<Value>, metadata: CacheMetadata, report: Reporter<Value>): Promise<Value>;
